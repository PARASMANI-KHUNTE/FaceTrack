// const multer = require('multer');

// const storage = multer.memoryStorage(); // Store files in memory before uploading to Google Cloud
// const upload = multer({ storage });

// module.exports = upload.single('profileImage'); // Expecting `profileImage` as the form field name
